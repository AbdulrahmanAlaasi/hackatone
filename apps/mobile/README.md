# @hackatone/mobile

Expo React Native (Expo Router) — participant app.

```bash
cp .env.example .env.local
npm run start    # opens Expo dev tools; scan QR with Expo Go
```

Auth, QR, team, and submission screens land in prompts 7–11.
