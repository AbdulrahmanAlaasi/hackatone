# @hackatone/web

Next.js 14 (App Router) — organizer dashboard, public registration, leaderboards, judging UI.

```bash
cp .env.example .env.local
npm run dev   # http://localhost:3000
```

Routes are scaffolded incrementally in prompts 3–6, 9, 10.
